services:
  - type: web
    name: newsseeker
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn -w 4 main:app"
    envVars:
      - key: SECRET_KEY
        value: change-me-please
      - key: ADMIN_PASSWORD
        value: admin
